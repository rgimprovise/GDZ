# Alternate Postgres port for VPS (in case 5432 is occupied).
# Usage: docker compose -f docker-compose.yml -f docker-compose.vps-ports.yml up -d --build

services:
  postgres:
    ports:
      - "5433:5432"
